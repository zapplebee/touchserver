doctype html
html(lang="en",ng-app="app")
  head
    meta(charset="utf-8")
    meta(name="viewport",content="width=device-width")
    link(href="https://fonts.googleapis.com/css?family=Open+Sans:300,700",rel="stylesheet")
    link(href="/style/main.css",rel="stylesheet")
    script(src="/script/angular.js")
    script(src="/script/main.js")
    title Premier Pet Lodge
  body
    header(ng-controller="navctrl")
      
      a#logo(href="#home",ng-class="isTop()") Premier Pet Lodge
      nav
        a.button(href="#booking") Reserve Now
        a.map(href="#booking")
    div.pane.doggo.coverimage.flex
      p.announce Central Minnesota's newest family owned and operated pet lodging service is coming soon.
      div.cutout
    div.pane.triad
      div.uniad TEst
      div.uniad TEst
      div.uniad TEst